2026-05-01: No blocking issues; partial edit in `src/camera.rs` was repaired by restoring the struct header and removing stale pointer plumbing.
2026-05-01: Unresolved for this QA pass: `classic_over_world_generator.rs` still contains cleanup regressions (`static mut NOISE_GEN`, `p_chunk` identifiers).
